{
  "name": "csv-estimate-to-csv-import-jira",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun --watch index.ts",
    "test": "bun test",
    "tw": "bun test:watch",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "axios": "^1.7.4",
    "csv-parser": "^3.0.0",
    "fast-csv": "^5.0.1",
    "fs": "^0.0.1-security"
  }
}